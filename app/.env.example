# Mobile App Environment Variables
# Copy this file to .env and fill in your values

# Backend API Configuration
# For local development with Expo:
# - Use your computer's local IP address (e.g., http://192.168.1.100:3000)
# - Or use http://localhost:3000 if running on emulator/simulator
# For production:
# - Use your production API URL (e.g., https://api.roadsauthority.na)

API_BASE_URL=http://localhost:3000

# API Timeout (in milliseconds)
API_TIMEOUT=10000

# App Configuration
APP_NAME=Roads Authority Namibia
APP_VERSION=1.0.0

# Feature Flags (optional)
ENABLE_CHATBOT=true
ENABLE_NOTIFICATIONS=true

# Development Settings
DEBUG_MODE=false

# ==============================================
# NOTES FOR MOBILE DEVELOPMENT
# ==============================================
# 1. When testing on a physical device, replace localhost with your computer's IP address
#    Example: API_BASE_URL=http://192.168.1.100:3000
# 
# 2. To find your IP address:
#    - Windows: Run 'ipconfig' in command prompt
#    - Mac/Linux: Run 'ifconfig' or 'ip addr' in terminal
#    - Look for your local network IP (usually starts with 192.168.x.x or 10.0.x.x)
#
# 3. Make sure your device and computer are on the same network
#
# 4. For Android emulator, you can use http://10.0.2.2:3000 to access localhost
#
# 5. For iOS simulator, you can use http://localhost:3000
